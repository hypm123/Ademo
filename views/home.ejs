<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: #fff;
        }
        .header {
            font-size: 24px;
            margin: 16px 0;
        }
        .button {
            background-color: #f0f0f0;
            color: #333;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            margin: 10px; /* Added margin for spacing between buttons */
            width: 200px; /* Width of the buttons */
        }
        .button:hover {
            background-color: #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="header">Xin chào</div>
    <button class="button" onclick="location.href='/send';">Send</button>
    <button class="button" onclick="location.href='/settings';">Settings</button>
</body>
</html>
